// Construct - new instances of a class.
// Extend - class to create a new subtype.
// Implement - class or mixin's interface.

// abstract = To define a class that doesn't require a full implementation of its entire interface
// Abstract classes cannot be constructed from any library, whether its own or an outside library.

abstract class myWorld{
  void whereIsShe();
}